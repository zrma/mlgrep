# mlgrep Binary Distribution

This archive contains the native `mlgrep` command for one supported platform.

- `bin/mlgrep`: executable binary
- `LICENSE-MIT`, `LICENSE-APACHE`: dual-license terms
- `README.md`: this distribution guide

Move `bin/mlgrep` onto `PATH`, or use the release `install.sh` to verify and install the archive.
